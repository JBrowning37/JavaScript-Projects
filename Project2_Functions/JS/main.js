function Concatenating() { //when function is called prints sentence that is concatenated.//
    var sentence = "This is the first part"; //First variable in concatenation//
    sentence += " and this is the last part"; //Second part of concatenation//
    document.getElementById("Concatenate").innerHTML = sentence; 
}

